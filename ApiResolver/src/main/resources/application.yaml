grpc:
    server:
        port: ${GRPC_PORT:7322}
        enable-reflection: true
server:
  address: ${SERVER_ADDRESS:0.0.0.0}
  port: ${SERVER_PORT:7321}

spring:
  import:
    optional:
      file: .env[.properties]
  application:
    name: ApiResolver
  ai:
    ollama:
      base-url: ${OLLAMA_SERVER:http://localhost:11434}
      init:
        pull-model-strategy: when_missing
        timeout: ${TIME_OUT:10000}
      chat:
        options:
          model: ${CHAT_MODEL:tinydolphin}
      embedding:
        options:
           model: ${EMBEDDINGS_MODEL:nomic-embed-text}
    vectorstore:
      pgvector:
        initialize-schema: true
        dimensions: ${VECTOR_STORE_DISMENSIONS:768}
        index-type: ${VECTOR_STORE_INDEX_TYPE:hnsw}
        schema-validation: true
  datasource:
    url: ${POSTGRES_URL:jdbc:postgresql://localhost:5432/spring}
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:koti21}
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: ${HIBERNATE_AUTO:create}